import React from "react";

const About = () => {
  return (
    <section className="section about-section">
      <h1 className="section-title">about me</h1>
      <p>
        Piccolo progetto per Plansoft utilizzando C# e Dapper (object mapper per
        .NET) per quanto riguarda il backend, mentre Reactjs per la parte
        frontend.
      </p>
    </section>
  );
};

export default About;
